# Hello Azure! Use Azure Compute to bootstrap a CNAB

This is a Duffle bundle that uses a VM to start instead of a container.

To run:
```
$ duffle install --driver azvm hello-azure -f helloazure/cnab/bundle.json
```